repositories {
    mavenCentral()
}

configurations.all {
    resolutionStrategy {
        preferProjectModules()
    }
}

tasks.withType(Test).configureEach {
    distribution {
        enabled = false
        maxRemoteExecutors = 2
    }
}
