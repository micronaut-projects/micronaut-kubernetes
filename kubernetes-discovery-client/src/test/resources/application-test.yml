micronaut:
  config-client:
    enabled: true
  http:
    services:
      kubernetes:
        path: /api/v1
---
kubernetes:
  client:
    namespace: micronaut-kubernetes
    host: localhost
    port: 8001
    secure: false