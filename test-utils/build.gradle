plugins {
    id 'io.micronaut.build.internal.kubernetes-test-module'
}

dependencies {
    annotationProcessor(libs.micronaut.inject.groovy)

    implementation(libs.groovy)
    api(libs.micronaut.inject.groovy)
    api(libs.micronaut.http.client)
    api(libs.kubernetes.client)
    api(libs.spock.core)
    api(libs.micronaut.test.spock)
    api(libs.logback.classic)
}

repositories {
    mavenCentral()
}
