micronaut:
  server:
    port: 8082
  application:
    name: micronaut-kubernetes-example-operator-openapi

kubernetes:
  client:
    operator:
      leader-election:
        lock:
          resource-kind: lease
          resource-name: lease-lock
